# js-mini-quiz-game
An interactive multiple-choice quiz game built with Vanilla JavaScript. The app uses a function that accepts the user's selected answer as a parameter via an onclick event. Using if / else logic, the script validates the answer, prints instant feedback ('Correct' or 'Wrong'), and dynamically updates text colors using CSS.
